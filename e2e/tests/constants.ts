// App Routes
export const ROUTES = {
  LOGIN: '/auth/login',
  DASHBOARD: '/dashboard',
  ROLES: '/roles'
} as const;

// API Endpoints
export const API_ENDPOINTS = {
  ROLES: '/permissions/dashboard'
} as const; 