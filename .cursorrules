# BAWES ERP API SDK Rules
# This file helps Cursor AI understand the BAWES ERP API SDK structure and usage

# SDK Import Pattern
import { Configuration, DefaultApi, AuthenticationApi } from '@bawes/erp-api-sdk';

# SDK Configuration
const config = new Configuration({
    basePath: string,  # API base URL
    apiKey: string     # API key for authentication
});

# Available API Classes
- DefaultApi: Base API client
- AuthenticationApi: Authentication operations
  - login(email: string, password: string)
  - register(email: string, password: string, nameEn: string, nameAr: string)
  - verifyEmail(email: string, code: string)
  - refresh(refresh_token: string)
  - logout(refresh_token: string)

# Common Usage Pattern
```typescript
const config = new Configuration({
    basePath: process.env.NEXT_PUBLIC_ERP_API_URL,
    apiKey: process.env.NEXT_PUBLIC_ERP_API_KEY
});

const api = new DefaultApi(config);
const authApi = new AuthenticationApi(config);
```

# Environment Variables Required
NEXT_PUBLIC_ERP_API_URL=string
NEXT_PUBLIC_ERP_API_KEY=string

# SDK Response Type
All API methods return AxiosPromise<T> 