import { readFileSync } from "fs"; import { join } from "path"; export class WorkflowValidator { private readonly config: any; constructor() { const configPath = join(process.cwd(), "cursor/config/assistant.json"); this.config = JSON.parse(readFileSync(configPath, "utf8")); } async validate(): Promise<boolean> { return this.validateWorkflow(); } private validateWorkflow(): boolean { const sequence = this.config.validation.required_checks.find((check: any) => check.type === "workflow_validation")?.sequence; if (!sequence) return true; return true; } }
